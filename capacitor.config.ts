import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pixieedlabo.app',
  appName: 'PiXiEEDraw',
  webDir: '.',
  server: {
    androidScheme: 'https'
  }
};

export default config;


